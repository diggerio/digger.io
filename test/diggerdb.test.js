var digger = require('../src');
var data = require('./fixtures/data');
var async = require('async');

describe('diggerdb', function(){

  it('should select containers', function(done) {
  
		done();
		
		/*
		var db = digger.database.digger({
			collection:'test',
			reset:true
		})

    var container = digger.supplychain(db);
    container.diggerwarehouse('/api');

    var data = digger.create(data.simplexml);

    container
			.append(data)
			.ship(function(){

				container('product')
					.ship(function(products){
						products.count().should.equal(2);
						done();
					})

			})
    */
  })

})